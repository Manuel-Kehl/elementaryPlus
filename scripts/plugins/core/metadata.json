{
    "label": "Core icon theme",
    "icon": "preferences-desktop",
    "description": "The core elementary+ icon theme",
    "scripts": {
        "install": {
            "command": "python install.py --install"
        },
        "remove": {
            "command": "python install.py --remove"
        }
    }
}
